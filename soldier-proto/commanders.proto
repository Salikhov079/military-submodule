syntax = "proto3";

option go_package = "genprotos/soldier";

package commander;

import "soldier.proto";

service CommanderService {
    rpc Create(CommanderReq) returns (soldier.Void);
    rpc Update(Commander) returns (soldier.Void);
    rpc Delete(soldier.ById) returns (soldier.Void);
    rpc Get(soldier.ById) returns (Commander);
    rpc GetAll(CommanderReq) returns (AllCommanders);
}

message CommanderReq {
    string name = 2;
    string email = 3;
    string date_of_birth = 4;
    string phone_number = 5;
    string position = 6;
}

message Commander {
    string id = 1;
    string name = 2;
    string email = 3;
    string date_of_birth = 4;
    string phone_number = 5;
    string position = 6;
}

message AllCommanders {
    repeated Commander commanders = 1;
}