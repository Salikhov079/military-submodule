syntax = "proto3";

option go_package = "genprotos/soldier";

package group;

import "soldiers.proto";

service GroupService {
    rpc Create(GroupReq) returns (Void);
    rpc Update(Group) returns (Void);
    rpc Delete(ById) returns (Void);
    rpc Get(ById) returns (Group);
    rpc GetAll(GroupReq) returns (AllGroups);
}

message GroupReq {
    string name = 1;
    string department_id = 2;
    string commanders_id = 3;
}

message Group {
    string id = 1;
    string name = 2;
    string department_id = 3;
    string commanders_id = 4;
}

message AllGroups {
    repeated Group groups = 1;
}
