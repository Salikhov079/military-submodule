syntax = "proto3";

option go_package = "genprotos/military";

package fuel;

import "techniques.proto";

service FuelService {
    rpc Create(FuelReq) returns (Fuel);
    rpc Update(Fuel) returns (Fuel);
    rpc Delete(ById) returns (Void);
    rpc Get(ById) returns (Fuel);
    rpc GetAll(FuelReq) returns (Fuel);
}

message FuelReq {
    string type = 2;
    int32 quantity = 3;
}

message Fuel {
    string id = 1;
    string type = 2;
    int32 quantity = 3;
}

message AllFuels {
    repeated Fuel fuels = 1;
}